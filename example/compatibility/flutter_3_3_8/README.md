# Flutter 3.3.8 compatibility harness

This minimal application keeps the native project generated by Flutter 3.3.8.
It verifies the package floor independently from the modern gallery in
`example/`.

Run it with the official Flutter 3.3.8 SDK and JDK 17:

```sh
flutter pub get
flutter analyze
flutter test
flutter build apk --debug
```
