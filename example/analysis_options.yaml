include: package:flutter_lints/flutter.yaml

# The floor harness is an independent Dart package and is analyzed from its
# own working directory in the minimum CI lane.
analyzer:
  exclude:
    - compatibility/**

linter:
  rules:
    # Chart event callbacks intentionally print their payload in this example.
    avoid_print: false
